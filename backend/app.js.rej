diff a/backend/app.js b/backend/app.js	(rejected hunks)
@@ -16,6 +16,12 @@
   })
 );
 
+app.use(
+  express.json({
+    limit: "50mb",
+  })
+);
+
 const server = app.listen(3000, () => {
   console.log("Server started.");
 });
